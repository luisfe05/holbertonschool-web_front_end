# Advanced CSS

## Description
This project focuses on advanced styling techniques using CSS3. It covers establishing global CSS variables, custom typography, advanced layout mechanics using floats and grid systems, responsive utilities, card components, pseudo-classes, pseudo-elements, custom transitions, animations, and vendor prefixing.

## Tasks Breakdown

| Task | Description | Output Files |
| :--- | :--- | :--- |
| **0. Let's get some images!** | Setup initial HTML starter file and download required project images. | `index.html`, `images/` |
| **1. Structure the page** | Structure header, main, and footer sections. | `styles/1-style.css` |
| **2. Smooth scrolling** | Add smooth scrolling behavior to html. | `styles/2-style.css` |
| **3. Center section titles** | Align section headers and titles. | `styles/3-style.css` |
| **4. Typography** | Set primary base font families and weights. | `styles/4-style.css` |
| **5. Add custom properties** | Create CSS variables for primary and dark theme colors. | `styles/5-style.css` |
| **6. Custom font sizes** | Define font-size variables and apply rem units. | `styles/6-style.css` |
| **7. Line height** | Add line-height variables for readability. | `styles/7-style.css` |
| **8. Custom fonts** | Import Google Fonts (Open Sans & Raleway). | `styles/8-style.css` |
| **9. Section tagline styling** | Style tagline elements with uppercase transform and brand color. | `styles/9-style.css` |
| **10. Links styling** | Define pseudo-classes (`:link`, `:visited`, `:hover`, `:active`) for navigation links. | `styles/10-style.css` |
| **11. Card category styling** | Apply category styling to project cards. | `styles/11-style.css` |
| **12. Visually hidden class** | Create utility class for screen readers and accessibility. | `styles/12-style.css` |
| **13. Section title margin** | Adjust margins for section titles. | `styles/13-style.css` |
| **14. Resetting CSS stylesheet** | Import `normalize.css` (v8.0.1) at top of stylesheet for browser consistency. | `styles/14-style.css` |
| **15. Add universal box-sizing** | Apply `box-sizing: border-box` across all elements and pseudo-elements. | `styles/15-style.css` |
| **16. Styling the container** | Style `.container` width (960px) and auto margins for horizontal alignment. | `styles/16-style.css` |
| **17. Adding padding to sections** | Define padding custom properties and apply them to `.section`, `.section-header`, `.section-body`, `.section-footer`, and `.footer`. | `styles/17-style.css` |
| **18. Customizing the navbar** | Style navigation menu layout, item margins, and link hover states. | `styles/18-style.css` |
| **19. Grid styling and custom variables** | Create `.row` and column classes (`.col-1-3`, `.col-1-2`) for responsive grid layout. | `styles/19-style.css` |
| **20. Clear context of the grid** | Add clearfix rule (`::after`) with `display: table` and `clear: both` to `.row`. | `styles/20-style.css` |
| **21. Simplify the col- selector** | Refactor grid system using attribute selector `[class*="col-"]`. | `styles/21-style.css` |
| **22. Add dark theme to sections** | Style `[data-section-theme="dark"]` overriding text/title variables to white and background to black. | `styles/22-style.css` |
| **23. Fix issues for dark theme** | Fix color inheritance for `.footer-address` and `.social-link > svg`. | `styles/23-style.css` |
| **24. Add background and hover state to services** | Style `.card-services` title and block links with hover color changes. | `styles/24-style.css` |
| **25. Add border to the button** | Define button variables, styling `.button` and hover states in light/dark themes. | `styles/25-style.css` |
| **26. Add border radius to images** | Style testimonial avatars with 50% border-radius and center alignment. | `styles/26-style.css` |
| **27. Styling the section hero** | Style `.section-hero` background sizing, title bottom margin, and inner padding. | `styles/27-style.css` |
| **28. Fixing the header and logo** | Add header positioning variables and absolute positioning for `.header-logo a`. | `styles/28-style.css` |
| **29. Styling nav link animation** | Add absolute pseudo-element (`::before`) animated line beneath nav links on hover. | `styles/29-style.css` |
| **30. Fix the works section** | Style `.card-work` overlay, image scaling, and card inner hover states. | `styles/30-style.css` |
| **31. Add quotes decoration on testimonials** | Add pseudo-element (`::before`) quote mark (`\201C`) to testimonial quotes. | `styles/31-style.css` |
| **32. Incorporating transitions** | Define cubic-bezier duration variables and apply hover scale transforms & transitions across cards, buttons, and navigation. | `styles/32-style.css` |

## Author
* **Luis Gonzalez** - Holberton School